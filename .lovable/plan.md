1. Update `src/components/sections/Footer.tsx`: add a "Powered by HLPR Ministries" line (linking to `https://ministries.hlpr.io/`) inside the bottom copyright bar, keeping the existing © line.
2. Update `src/config/site.ts`: remove the LinkedIn entry from the `contact.socials` array so it no longer renders in the footer or anywhere else that consumes the config.